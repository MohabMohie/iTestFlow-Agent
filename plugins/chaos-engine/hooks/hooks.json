{
  "hooks": {
    "PostToolUse": [
      {
        "hooks": [
          {
            "command": "python -c \"import os, pathlib, runpy; root = os.environ.get('CLAUDE_PLUGIN_ROOT', ''); path = pathlib.Path(root) / 'hooks' / 'guard.py' if root else None; path = path if path is not None and path.is_file() else None; paths = ((pathlib.Path('.').resolve(),) + tuple(pathlib.Path('.').resolve().parents)); path = path or next((candidate for parent in paths for candidate in (parent / '.chaos-engine' / 'hooks' / 'guard.py', parent / 'plugins' / 'chaos-engine' / 'hooks' / 'guard.py') if candidate.is_file()), None); path = path or pathlib.Path('.chaos-engine/hooks/guard.py').resolve(); runpy.run_path(str(path), run_name='__main__')\"",
            "timeout": 5,
            "type": "command"
          }
        ],
        "matcher": "Bash|PowerShell|shell_command"
      }
    ],
    "PreToolUse": [
      {
        "hooks": [
          {
            "command": "python -c \"import os, pathlib, runpy; root = os.environ.get('CLAUDE_PLUGIN_ROOT', ''); path = pathlib.Path(root) / 'hooks' / 'guard.py' if root else None; path = path if path is not None and path.is_file() else None; paths = ((pathlib.Path('.').resolve(),) + tuple(pathlib.Path('.').resolve().parents)); path = path or next((candidate for parent in paths for candidate in (parent / '.chaos-engine' / 'hooks' / 'guard.py', parent / 'plugins' / 'chaos-engine' / 'hooks' / 'guard.py') if candidate.is_file()), None); path = path or pathlib.Path('.chaos-engine/hooks/guard.py').resolve(); runpy.run_path(str(path), run_name='__main__')\"",
            "timeout": 5,
            "type": "command"
          }
        ],
        "matcher": "Bash|PowerShell|shell_command"
      }
    ],
    "SessionStart": [
      {
        "hooks": [
          {
            "command": "python -c \"import os, pathlib, runpy; root = os.environ.get('CLAUDE_PLUGIN_ROOT', ''); path = pathlib.Path(root) / 'hooks' / 'guard.py' if root else None; path = path if path is not None and path.is_file() else None; paths = ((pathlib.Path('.').resolve(),) + tuple(pathlib.Path('.').resolve().parents)); path = path or next((candidate for parent in paths for candidate in (parent / '.chaos-engine' / 'hooks' / 'guard.py', parent / 'plugins' / 'chaos-engine' / 'hooks' / 'guard.py') if candidate.is_file()), None); path = path or pathlib.Path('.chaos-engine/hooks/guard.py').resolve(); runpy.run_path(str(path), run_name='__main__')\"",
            "timeout": 5,
            "type": "command"
          }
        ],
        "matcher": "startup|resume|clear|compact"
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "command": "python -c \"import os, pathlib, runpy; root = os.environ.get('CLAUDE_PLUGIN_ROOT', ''); path = pathlib.Path(root) / 'hooks' / 'guard.py' if root else None; path = path if path is not None and path.is_file() else None; paths = ((pathlib.Path('.').resolve(),) + tuple(pathlib.Path('.').resolve().parents)); path = path or next((candidate for parent in paths for candidate in (parent / '.chaos-engine' / 'hooks' / 'guard.py', parent / 'plugins' / 'chaos-engine' / 'hooks' / 'guard.py') if candidate.is_file()), None); path = path or pathlib.Path('.chaos-engine/hooks/guard.py').resolve(); runpy.run_path(str(path), run_name='__main__')\"",
            "timeout": 5,
            "type": "command"
          }
        ]
      }
    ],
    "SubagentStop": [
      {
        "hooks": [
          {
            "command": "python -c \"import os, pathlib, runpy; root = os.environ.get('CLAUDE_PLUGIN_ROOT', ''); path = pathlib.Path(root) / 'hooks' / 'guard.py' if root else None; path = path if path is not None and path.is_file() else None; paths = ((pathlib.Path('.').resolve(),) + tuple(pathlib.Path('.').resolve().parents)); path = path or next((candidate for parent in paths for candidate in (parent / '.chaos-engine' / 'hooks' / 'guard.py', parent / 'plugins' / 'chaos-engine' / 'hooks' / 'guard.py') if candidate.is_file()), None); path = path or pathlib.Path('.chaos-engine/hooks/guard.py').resolve(); runpy.run_path(str(path), run_name='__main__')\"",
            "timeout": 5,
            "type": "command"
          }
        ]
      }
    ],
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "command": "python -c \"import os, pathlib, runpy; root = os.environ.get('CLAUDE_PLUGIN_ROOT', ''); path = pathlib.Path(root) / 'hooks' / 'guard.py' if root else None; path = path if path is not None and path.is_file() else None; paths = ((pathlib.Path('.').resolve(),) + tuple(pathlib.Path('.').resolve().parents)); path = path or next((candidate for parent in paths for candidate in (parent / '.chaos-engine' / 'hooks' / 'guard.py', parent / 'plugins' / 'chaos-engine' / 'hooks' / 'guard.py') if candidate.is_file()), None); path = path or pathlib.Path('.chaos-engine/hooks/guard.py').resolve(); runpy.run_path(str(path), run_name='__main__')\"",
            "timeout": 5,
            "type": "command"
          }
        ]
      }
    ]
  }
}
